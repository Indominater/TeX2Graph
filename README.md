# Proogh

Proogh is a single-file browser app for visualizing dependencies between theorem-like blocks in TeX documents.

Open `proof-dependency-map.html` in a browser, then use `Open TeX` to load a source file and explore the resulting proof dependency graph.
