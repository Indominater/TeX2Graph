# TeX2Graph

TeX2Graph is a desktop and mobile browser app for visualizing dependencies between theorem-like blocks in TeX documents.

Use the hosted app at <https://indominater.github.io/TeX2Graph/>, then use `Upload TeX` to load a source file and explore the resulting proof dependency graph.
